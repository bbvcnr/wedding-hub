/**
 * Re-export useTheme for convenience
 * The useTheme hook now includes colors, so you can use:
 * const { isDark, colors, theme, setTheme, toggleTheme } = useTheme();
 */
export { useTheme } from '@/src/components/theme/theme-provider';

